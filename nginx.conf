server {
    listen 8080;

    location = /healthz {
        return 200 "ok";
    }

    location = /vcs {
        return 301 /vcs/;
    }

    location /vcs/ {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /vcs/index.html;
    }
}
